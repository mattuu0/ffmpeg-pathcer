[package]
name = "dummy-tcp-server"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "dummy-tcp-server"
path = "src/main.rs"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }

[profile.release]
codegen-units = 1
lto = true
opt-level = 3
strip = true
