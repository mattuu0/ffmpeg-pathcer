/target
poc/capture-helper/target/
ffmpeg-master-latest-win64-lgpl-shared/
poc/desktop-shot/output/
/ffmpeg/
output/*.dll

# Verification/benchmark results, logs, and recordings -- generated by
# running poc/*.py, not source.
poc/verify-matrix/
poc/**/__pycache__/
poc/*.log
poc/*.out
/test_output.log

# ddagrab_proxy.log lands next to the proxy DLL when it's run from the repo
# root (e.g. pasted here by the user for debugging); the ffmpeg-side log
# report-tool paths (rust-castsender etc.) are similarly pasted diagnostics,
# not source.
/ddagrab_proxy.log
/Users*ffmpeg-level*

# PsExec/PAExec -- fetched locally per-machine to relaunch ffmpeg.exe as
# SYSTEM (see send_stream.py's --psexec-path / --no-system-relaunch), not
# something this repo redistributes.
poc/latency-poc/windows-sender/paexec.exe
poc/latency-poc/windows-sender/PsExec*.exe
